[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "comext_wrapper"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["requests>=2.28", "pandas>=1.5"]

[tool.hatch.build.targets.wheel]
packages = ["src/comext_wrapper"]
